apiVersion: v1
kind: Service
metadata:
  name: progress-monitoring
  labels:
    app: progress-monitoring
spec:
  type: ClusterIP
  selector:
    app: progress-monitoring
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
      name: http

