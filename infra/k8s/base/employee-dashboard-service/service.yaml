apiVersion: v1
kind: Service
metadata:
  name: employee-dashboard-service
  labels:
    app: employee-dashboard-service
spec:
  selector:
    app: employee-dashboard-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
