apiVersion: v1
kind: Service
metadata:
  name: employee-dashboard-service
  labels:
    app: employee-dashboard-service
    monitoring: enabled
spec:
  selector:
    app: employee-dashboard-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  type: ClusterIP
