apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: autonova

resources:
  - ../../base

patchesStrategicMerge:
  - patches/image-tags.yaml
  - patches/resources.yaml

# Example: dev-specific image overrides and resource tuning
